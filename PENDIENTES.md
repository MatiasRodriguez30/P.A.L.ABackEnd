# Pendientes

## Modelo

- Revisar y validar el ABM de `TipoAviso` y `SubTipoAviso` con la relacion unidireccional `TipoAviso -> SubTipoAviso`.

## Flujos

- Implementar el catalogo para que el reclutador pueda consultar `TipoAviso` con sus `SubTipoAviso`.
- Implementar la seleccion historica de `AvisoTipoAviso` y `AvisoTipoAvisoSubTipoAviso`.
- Validar la regla de negocio: un solo `AvisoTipoAviso` activo por `Aviso` y por `TipoAviso`.
